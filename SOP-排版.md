# SOP · 笔记站排版

> README 讲**怎么用**，这份讲**为什么这么定**和**踩过什么坑**。
> 改样式、改出图、加新笔记类型之前读一遍，能省掉我已经返工过的几轮。
>
> 小红书卡片那条线是另一套数字，在
> `Obsidian Vault/EngD-Worksapce/Study/VLA学习主线/Publish/SOP-卡片排版.md`。

---

## 0. 一条主线

**所有尺寸决策，换算成「读者屏幕上的 CSS 像素」再判断。**

不看 pt、不看 figsize、不看原图分辨率。同一个 15pt 字，在一张 1000px 宽的图上
和一张 1950px 宽的图上，缩放后能差 1.6 倍——只看 pt 永远调不准。

---

## 1. 比例体系

| 量 | 值 | 依据 |
|---|---|---|
| 正文栏 `--measure` | 48rem / 768px | ÷17px ≈ 45 字/行。中文 40~46 字最舒服 |
| 图区 `--wide` | 68rem / 1088px | = 两张图并排 + 间距 |
| 正文 `--fs` / `--lh` | 17px / 1.85 | 中文行高要比英文大，1.6 会挤 |
| 图内「正文级」字 | 15px | 正文的 0.88 |
| 图内最小字 | ≥ 12px | 图注级 |

**英文排版那套「45~75 字符/行」不能照搬到中文。** 汉字是方块、信息密度高，
75 字符宽的栏放中文是 75 个字，一行扫过去眼睛要回跳。

### 图内字为什么是 15px 而不是 17px

**图里的文字是标注，不是阅读流。** 惯例上就该比正文小一号。

我一开始硬对齐到 17px，后果是图被撑大到每张独占一行、页面瘦而散，
横向空间全浪费在图两侧的留白上。降到 15px 之后图小了 12%，
中位宽从 598px 降到 529px，相邻两张才放得下并排。

---

## 2. 图的显示宽度：由图内字号反推

```
显示宽 = 15px × 原图宽 / (图内正文级字号pt × dpi/72)
```

**三种做法里只有这种能让基准一致**：

| 做法 | 结果 |
|---|---|
| 统一 pt（比如全图 ≥ 15pt） | 画幅不同 → 缩放不同 → 页面上 13~40px 不等 ❌ |
| 统一显示宽（704 / 960 两档） | 同上，只是换了个方向差 ❌ |
| 只兜最小字 ≥ 15px | 只管下限，主体落在 22~29 还是 15~26 完全不管 ❌ |
| **按图内字号反推显示宽** | 每张图的正文级都正好 15px ✅ |

「正文级字号」取**按字符数加权的中位数**：图里标题只有一行、主标签有几十个，
直接取中位数会被零散的大字拉偏。

### 两边公式必须同步

这个宽度依赖图内字号，`build.py` 自己算不出来。流程是：

```
save_web()  算出 disp  →  写进 out_web/<组>/_web.json
build.py    读 _web.json  →  写进 <figure style="--w:...">
CSS         width: min(100%, var(--w))
```

**页面自己另算宽度就破坏了对齐。** 改任何一头都要改另一头。

---

## 3. 并排

md 里紧挨着的两张图（中间没有正文），宽度和放得下就并成一行，**顶对齐**。

判断只有一个算式（`build.py` 里的 `PAGE_W=1088, GAP=26, SQUEEZE=0.88`）：

```
两图宽度和 + 26  ≤  1088 / 0.88 = 1236   →  并排
                 >  1236                 →  各自独占
```

并排时统一缩到刚好 1088：`k = (1088 - 26) / (wa + wb)`，k 最小 0.86。
那时图内正文级字 15px × 0.86 ≈ 12.9px，压着 12px 下限，还能读。
**1236 就是「略超」和「再超」的分界**，不是感觉。

举例（RN01 实际）：602 + 460 + 26 = 1088 → 并排且不用缩；
578 + 528 + 26 = 1132 ≤ 1236 → 并排，缩到 96%；
若两张都是 640，1306 > 1236 → 各自独占。

- 底对齐会让矮的那张沉下去，两张图的标题不在一条线上，很难看。
- 窄屏（< 46rem）自动拆回上下排列。
- 判断**纯按宽度**，不管语义。遇到不该并的，在 md 里两张图中间加一行文字就拆开了。

---

## 4. 踩过的坑

### 4.1 CSS

| 坑 | 症状 | 规则 |
|---|---|---|
| `transform` 冲突 | 宽图整个跑到右边 | 淡入动画的 `transform:none` 会覆盖居中用的 `translateX(-50%)`。居中用 `margin` 算，别用 transform |
| `body` 不透明背景 | 网格层完全看不见 | 负 z-index 的伪元素在父元素背景**之下**。底色放 `html`，`body` 透明 |
| 目录做成左侧固定栏 | 被图压住 | 正文图会挣脱到 68rem，左栏必然被压。改成右缘细线，hover 展开 |
| mask 渐隐范围 ≥ 内容边距 | 边上的节点/标签半透明 | 渐隐宽度要**窄于**内容的 padding |
| grid 里的 `max-height:100%` | 点开看大图时图溢出底部、看着像「图偏下」 | grid 行高默认 auto，子元素的百分比 max-height 和「行高由内容定」互相依赖，浏览器当 none。容器加 `grid-template-rows:minmax(0,1fr)`，子元素用 `calc(100vh - padding*2)` 这种绝对值兜底 |

### 4.2 JS

| 坑 | 症状 | 规则 |
|---|---|---|
| `d3.transition` 改 opacity | 无头浏览器截图里元素全不见 | 虚拟时间不推进 rAF，动画停在 opacity:0。入场动画一律用 CSS keyframes |
| 力导向图无层级约束 | 有向依赖揉成一团毛线 | 按拓扑深度锁一个轴，力只解决同层拥挤 |

### 4.3 出图

| 坑 | 规则 |
|---|---|
| `bbox_inches="tight"` 下改 figsize | 对最终比例没用，输出会被裁到内容。要改比例改内容布局 |
| 放大字号后重存再迭代 | 字大 → bbox 大 → 缩放小 → 字更大，滚雪球。只重存一轮 |
| `axis("off")` 的刻度标签 | `findobj` 找得到但画不出来，会被误判成「最小字」。它们的 `.axes` 是 None，要从每个 axis 那头收集 id 排除 |
| 中文和 `$...$` 写在同一字符串 | 整串走数学字体，中文变豆腐块。公式纯 ASCII，中文另起一条 text |

---

## 5. 加新笔记时的检查

```bash
python build.py --serve
```

1. **图内字和正文一个基准吗**：截一张「文字 + 图」相接的图，1:1 看图里的标签
   和正文是不是差不多大。差很多说明 `_web.json` 没更新或页面没读它。
2. **有没有该并排却没并的**：`grep -c figrow notes/<id>/index.html`，
   对照 md 里紧挨着的图有几对。
3. **窄屏**：拉到 390px 宽，看有没有横向溢出、并排有没有拆回上下。
4. **断链**：
   ```python
   import pathlib, re
   for p in list(pathlib.Path('.').glob('*.html')) + list(pathlib.Path('.').glob('notes/*/index.html')):
       for m in re.findall(r'(?:href|src)="([^"#][^"]*)"', p.read_text()):
           if not m.startswith(('http','//','mailto')) and not (p.parent/m).exists():
               print('✗', p, m)
   ```

---

## 6. 两个出口的关系

| | 网页（主） | 小红书卡片（引流） |
|---|---|---|
| 目录 | `~/Documents/Repository/vla-notes/` | vault 的 `Publish/` |
| 出图 | `VLA_WEB=1 python run-all.py` → `out_web/` | `python run-all.py` → `out/` |
| 图内字基准 | 15px（页面正文 17px） | ≥ 22px（卡片正文 33px） |
| 为什么不同 | 图 450~1088px 显示、能点开看原图 | 图被缩进 912px 卡片、手机上看、放不大 |
| 约束 | 无分页，图任意高 | 1080×1440 硬分页，图不能切 |

同一份 md、同一批实验脚本，靠 `VLA_WEB` 环境变量切换。
